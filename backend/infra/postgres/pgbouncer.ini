[databases]
zenvy = host=haproxy port=5432 dbname=zenvy

[pgbouncer]
listen_port = 6432
listen_addr = 0.0.0.0
auth_type = trust
pool_mode = transaction
max_client_conn = 5000
default_pool_size = 25
reserve_pool_size = 10

